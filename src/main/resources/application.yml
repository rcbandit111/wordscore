server:
  port: 8000

spring:
  application:
    name: wordscore-engine
  profiles:
    active: dev
  datasource:
    url: jdbc:postgresql://185.185.126.15:5432/wordscore_engine
    driverClassName: org.postgresql.Driver
    username: admin
    password: 6y5t4r3e2w1q!Q
  jpa:
    generate-ddl: true
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQL95Dialect
    show-sql: true

simplejob:
  frequency: 1000
